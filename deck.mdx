export { default as theme } from "./theme";

<img src={require("file-loader!./src/assets/logo.png")} />

<p>Larissa Dantas</p>

---

# O que é

Git é um sistema de controle de versão de arquivos.

---

## Instalando git

Plataformas disponíveis:

- Windows
- Linux
- Mac

```
https://git-scm.com/downloads
```

---

## Configurando Ambiente

Global:

```
git config --global user.name "Larissa"
git config --global user.email "contato@prov.com"
```

Para visualizar suas configurações:

```
git config user.name
Larissa
```

Para visualizar TODAS as informações:

```
git config --list
```

Editor do git (opcional):

```
git config --global core.editor code
```

---

### SSH keys

- RSA, DSA, ECDSA e ED25519;

```
ssh-keygen -t rsa -b 4096 -C "email@hue.br"
```

-t [type]

-b [bits]

-C [comentário]

---

#### Copiando sua key:

-Mac OS:

```
pbcopy < ~/.ssh/id_rsa.pub
```

-WSL / GNU / Linux (requer o pacote xclip):

```
xclip -sel clip < ~/.ssh/id_ed25519.pub
```

-Git Bash no Windows:

```
cat ~/.ssh/id_ed25519.pub | clip
```

---

<img src={require("file-loader!./src/assets/ssh.png")} />

---

## Testando:

```
ssh -T git@gitlab.com
Bem-vindo ao GitLab @username!
```

---

## Inicializando um repositório

```
mkdir git-guide
cd git-guide/
git init

Initialized empty Git repository in ../../../git-guide/.git

cd .git/
ls
HEAD   branches    config     description   hooks    info  objects  refs
```

HEAD -> Branch padrão atual

branches -> Branches existentes no projeto

config -> configuração do repositorio

description -> Descrição do projeto

hooks -> Gatilhos para fazer certas ações

---

## Ciclo de vida dos status de seus arquivos

<img src={require("file-loader!./src/assets/lifecycle.png")} />

---

## Visualizando logs

```
git log
commit f0dada5e0c4f6c50bf2a4a57f6b39677827e5f06 (HEAD -> master, origin/master, origin/HEAD)
Author: Larissa <contato@contato.com>
Date:   Mon Sep 30 00:42:02 2019 -0300

    Insert lifecycle

git log --author="Larissa"
commit f0dada5e0c4f6c50bf2a4a57f6b39677827e5f06 (HEAD -> master, origin/master, origin/HEAD)
Author: Larissa <contato@contato.com>
Date:   Mon Sep 30 00:42:02 2019 -0300

    Insert lifecycle


git shortlog
Larissa (5):
      git guide structuring
      feat: Organizing structure
      feat: structuring
      feat: inserting ssh structure
      Insert lifecycle

git log --graph

git show f0dada5e0c4f6c50bf2a4a57f6b39677827e5f06
```

---

## Diff

Visualizando o que alterou

```
git diff
git diff --name-only
```

---

##
